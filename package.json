{
  "name": "ngrok-ssh",
  "version": "1.0.0",
  "main": "main.mjs",
  "scripts": {
    "prep:dev": "rm -rf dev && mkdir dev",
    "main:dev": "npm run prep:dev && node --env-file=.env main.mjs && npm run cleanup:dev",
    "post:dev": "node --env-file=.env post.mjs",
    "cleanup:dev": "killall tmux ngrok sshd"
  },
  "engines": {
    "node": ">=20.6.0"
  },
  "author": {
    "name": "Tim Shaker",
    "email": "tim@tmshkr.com",
    "url": "https://github.com/tmshkr"
  },
  "license": "MIT"
}
